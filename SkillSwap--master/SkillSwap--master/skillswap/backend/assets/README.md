Place your exact invitation image here as `invite-email.png`.

Used by: `INVITE_EMAIL_IMAGE_PATH=assets/invite-email.png`
Endpoint: `POST /api/invitations/send`

Notes:

- Recommended width: 760px (or any proportional size)
- The image is sent inline in email via CID, so recipient sees the same design image.
